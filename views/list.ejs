<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vector transports-Home</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <style>
        .container {
            padding: 15px 0px 15px 15px;
        }
    </style>
</head>

<body>
    <div class="container mt-3 mb-1 text-center">
        <img src="/images/trucklogo.jpg" width="200" alt="trucklogo">
    </div>
    <div class="d-flex text-center align-center justify-content-center">
        <div class="mb-2 mt-2 d-flex justify-content-center"><a href="/new"><button class="btn bg-dark text-light" >नया डाक्यूमेंट</button></a></div>
        <form class="ml-2 mt-2 " action="/logout" method="POST">
            <button type="submit" class="btn btn-primary">
                लॉग-आउट
          </button>
        </form>
    </div>


    <% if(typeof data !== "undefined"){ %>

        <% Array.from(data).forEach((item)=>{ %>

            <div class="container d-flex flex-column bg-dark text-light" style="border: 1px solid black">
                <div class="item">
                    document ID:
                    <%= item._id %>
                </div>
                <div class="item">
                    डाक्यूमेंट कब बनाया गया:
                    <%= item.createdAt %>
                </div>
                <div class="item">
                    ट्रीप कब शुरू हुई:
                    <%= item.dateStarted %>
                </div>
                <div class="item">
                    ट्रीप कहाँ से शुरू हुई:
                    <%= item.origin %>
                </div>
                <div class="item">
                    ट्रीप कहाँ समाप्त हुई:
                    <%= item.destination %>
                </div>
                <div class="item">
                    ड्राइवर:
                    <%= item.driver %>
                </div>
                <div class="item">
                    कस्टमर:
                    <%= item.client %>
                </div>
                <div class="item">
                    ट्रीप कब समाप्त हुई:
                    <%= item.dateEnd %>
                </div>
                <div class="item">
                    यह डाक्यूमेंट किसने बनाया:
                    <%= item.createdBy %>
                </div>
                <div class="align-items-center">
                    <a class="mr-auto" href="/edit/<%= item._id %>"><button class="text-light btn-sm btn bg-secondary">एडिट</button></a>
                    <a class="mr-auto ml-3" href="/delete/<%= item._id %>"><button class="text-light btn-sm btn bg-danger" >डिलीट</button></a></div>
            </div>
            <% }) %>

                <% } %>
</body>

</html>