<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>neoGrapht</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>

<body>
    <% if(typeof data !== 'undefined'){ %>
        <div class="container mt-2">
            <div class="bg-dark text-light text-center">
                <div class="data">
                    ID:
                    <%= data._id %>
                </div>
                <div class="data">
                    created on:
                    <%= data.createdAt %>
                </div>
                <div class="data">
                    date started:
                    <%= data.dateStarted %>
                </div>
                <div class="data">
                    origin:
                    <%= data.origin %>
                </div>
                <div class="data">
                    destination:
                    <%= data.destination %>
                </div>
                <div class="data">
                    driver:
                    <%= data.driver %>
                </div>
                <div class="data">
                    client:
                    <%= data.client %>
                </div>
                <div class="data">
                    date endend:
                    <%= data.dateEnd %>
                </div>
            </div>
        </div>
        <form class="text-center" action="/<%= data._id %>?_method=DELETE" method="POST">
            <p>do you want to delete this precious piece of data?</p>
            <button class="btn btn-danger" type="submit">delete</button>
        </form>
        <% } %>
</body>

</html>